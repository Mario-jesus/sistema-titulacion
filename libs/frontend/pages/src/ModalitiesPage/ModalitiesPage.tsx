import { ModalitiesList } from '@features/modalities/ui';

/**
 * Página de Modalidades
 *
 * Componente de orquestación que renderiza el componente de la feature.
 * No contiene lógica de negocio, solo composición.
 */
export function ModalitiesPage() {
  return <ModalitiesList />;
}
