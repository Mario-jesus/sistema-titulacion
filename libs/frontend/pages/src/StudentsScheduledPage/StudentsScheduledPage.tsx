import { StudentsScheduledList } from '@features/students/ui';

/**
 * Página de Estudiantes Programados
 *
 * Componente de orquestación que renderiza el componente de la feature.
 * No contiene lógica de negocio, solo composición.
 */
export function StudentsScheduledPage() {
  return <StudentsScheduledList />;
}
