import { IngressEgressList } from '@features/ingress-egress/ui';

/**
 * Página de Ingreso y Egreso
 *
 * Componente de orquestación que renderiza el componente de la feature.
 * No contiene lógica de negocio, solo composición.
 */
export function IngressEgressPage() {
  return <IngressEgressList />;
}
