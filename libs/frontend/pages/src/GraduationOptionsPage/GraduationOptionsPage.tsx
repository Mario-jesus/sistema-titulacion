import { GraduationOptionsList } from '@features/graduation-options/ui';

/**
 * Página de Opciones de Titulación
 * 
 * Componente de orquestación que renderiza el componente de la feature.
 * No contiene lógica de negocio, solo composición.
 */
export function GraduationOptionsPage() {
  return <GraduationOptionsList />;
}
