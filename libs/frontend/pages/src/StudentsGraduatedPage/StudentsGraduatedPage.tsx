import { StudentsGraduatedList } from '@features/students/ui';

/**
 * Página de Estudiantes Titulados
 *
 * Componente de orquestación que renderiza el componente de la feature.
 * No contiene lógica de negocio, solo composición.
 */
export function StudentsGraduatedPage() {
  return <StudentsGraduatedList />;
}
