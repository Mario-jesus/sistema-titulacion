/**
 * Componentes auxiliares del router
 *
 * Componentes de utilidad para el sistema de routing:
 * - PageLoader: Componente de carga mientras se cargan las páginas lazy
 */

export { PageLoader } from './PageLoader';
