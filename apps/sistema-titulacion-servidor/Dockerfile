# This file is generated by Nx.
# Build the docker image with `npx nx docker:build @sistema-titulacion/sistema-titulacion-servidor`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `nx docker:run @sistema-titulacion/sistema-titulacion-servidor -p 3000:3000`.
#
FROM docker.io/node:25.1.0-alpine3.22

ENV HOST=0.0.0.0
ENV PORT=3000


WORKDIR /app

COPY dist .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.

RUN npm --omit=dev -f install

CMD [ "node", "main.js" ]
